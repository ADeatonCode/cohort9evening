import react from 'react';
import reactDOM from 'react-dom';

function panicButton() { 
    
    return (
        <button name='panic' id='button'>Panic Button</button>
    );
} 
const panicButton = panicButton();
export default panicButton;
